[project]
name = "rnacentral-probing-metadata"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "chardet<6",
    "linkml>=1.9.6",
    "pyyaml>=6.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]
